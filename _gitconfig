[user]
    name = Emlyn Corrin
    email = emlyn@corrin.name
[color]
    diff = auto
    status = auto
    branch = auto
    ui = auto
[core]
    pager = less -R
    autocrlf = input
    excludesfile = "~/.gitexcludes"
[push]
    default = matching
[alias]
    st = status -s
    co = checkout
    ci = commit
    unstage = reset HEAD --
    unadd = reset HEAD --
    tree = log --graph --oneline --decorate
    lg = log --graph --pretty=format:'%Cred%h%Creset %C(bold blue)<%an> %C(yellow)%d%Creset %s %Cgreen(%cr) %Creset'
